#include<iostream>
#include<set>

using namespace std;

int main()
{
    set<int> s;
    
    s.insert(1);   //to insert an element in set
    s.insert(2);
    s.insert(3);
    s.insert(5);
    
    for(int i:s)
    {
        cout<<i<<endl;     //printing all elements of set
    }
    
   s.insert(2);
   s.insert(2);
   s.insert(2);
   s.insert(3);
   s.insert(5);
   
   for(int i:s)
    {
        cout<<i<<endl;     //printing all elements of set
    }
    
    
 
    return 0;
}
