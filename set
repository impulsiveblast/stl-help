#include<bits/stdc++.h>
using namespace std;
 
 void print(set<string> s)
 {
     for(auto i:s)
     {
         cout<<i<<endl;
     }
     cout<<endl;
 }
 
int main()
{
    
  //sets are basically maps without values only keys
   set<string> s;     //set declaration
   
   s.insert("abc");                           //   whatever we store it gets stored in sorted order inside set

   s.insert("bsdk");
   s.insert("habro");
   s.insert("komediking");  //o(1)
   
   it=s.find("abc")    //o(1) returns the iterator where abc is stored
   
   
   
   print(s);
   
   //reverse pointer for set 
//right to left
   for (rit = s.rbegin(); rit != s.rend(); rit++) 
    {
       cout << *rit << " ";
     }
   
 


   return 0;
} 
